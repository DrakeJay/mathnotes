# Copy to .env and adjust. Defaults in app/config.py match docker-compose.yml.
DATABASE_URL=postgresql+psycopg://mathnotes:mathnotes@localhost:5433/mathnotes
ADMIN_PASSWORD=letmein
# Session lifetime and cookie flags; set COOKIE_SECURE=true behind HTTPS.
SESSION_TTL_HOURS=168
COOKIE_SECURE=false
